import type {Lesson} from '../lessons/lessons';import type {Result} from '../lessons/validators';
export function LessonPanel({lesson,results,index,count,completed,showingSolution,onPrevious,onNext,onSandbox,onSolution}:{lesson:Lesson;results:Result[];index:number;count:number;completed:boolean;showingSolution:boolean;onPrevious:()=>void;onNext:()=>void;onSandbox:()=>void;onSolution:()=>void}){return <header className="lesson"><div className="lesson-main"><span className="eyebrow">LESSON {index+1} OF {count}</span><h1>{lesson.title} {completed&&<span className="complete">Completed ✓</span>}</h1><p>{lesson.description}</p><p><b>Goal:</b> {lesson.objective}</p>{showingSolution&&<p className="solution-note">Example solution shown — return to your attempt whenever you are ready.</p>}</div><div className="lesson-meta"><div><b>Constraints</b>{lesson.constraints.map(x=><span key={x}>{x}</span>)}</div><div><b>Live tests</b>{results.map(r=><span className={r.passed?'pass':'fail'} key={r.label}>{r.passed?'✓':'○'} {r.label}</span>)}</div></div><nav><button onClick={onPrevious} disabled={!index}>← Previous</button><button onClick={onNext} disabled={index===count-1}>Next →</button><button className="solution-button" onClick={onSolution}>{showingSolution?'Return to attempt':'Show solution'}</button><button onClick={onSandbox}>Sandbox</button></nav></header>}
